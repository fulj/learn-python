# learn-python
```
git init
git remote add origin 
git rm -r –cached dirname //删除远程文件夹，但保留本地文件夹 
git commit -m ‘say something’ //提交操作，并添加描述 
git push origin master //推送

git rm -nr --cached .idea //列出要删除的内容
git rm -r --cached .idea //真删除
git commit -m "提交说明"
git push origin master
```